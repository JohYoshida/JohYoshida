<% include ../partials/header %>

<div class="ui middle aligned center aligned grid">

  <div class="six wide column">
    <h1 class="header">
      Edit <%= entry.name %>
    </h1>
    <div class="ui hidden divider"></div>

    <form class="ui small form" action="/journal/<%= entry._id %>?_method=PUT" method="POST">
      <div class="field">
        <label>Title</label>
        <input type="text" name="blog[title]" value="<%= entry.title %>">
      </div>
      <div class="field">
        <label>Image</label>
        <input type="text" name="blog[image]" value="<%= entry.image %>">
      </div>
      <div class="field">
        <label>Text</label>
        <input type="text" name="blog[text]" value="<%= entry.text %>">
      </div>
      <button class="ui button">
        Submit
      </button>
    </form>

    <!-- <form action="/journal/<%= entry._id %>?_method=PUT" method="POST">
      <div class="form-group">
        <input class="form-control" type="text" name="blog[name]" value="<%= entry.name %>">
      </div>
      <div class="form-group">
        <input class="form-control" type="text" name="blog[image]" value="<%= entry.image %>">
      </div>
      <div class="form-group">
        <input class="form-control" type="text" name="blog[text]" value="<%= entry.text %>">
      </div>
      <div class="form-group">
        <button class="btn btn-lg btn-primary btn-block">Submit</button>
      </div>
    </form> -->

    <a href="/journal">Go back without editing</a>
  </div>

</div>


<% include ../partials/footer %>
